{
  "name": "ghup-server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "format": "npx prettier \"./**/*.ts\" \"!./**/*.d.ts\" --write --no-semi --single-quote"
  },
  "dependencies": {
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
